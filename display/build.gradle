apply plugin: 'jaci.openrio.gradle.GradleRIO'

dependencies {
  compile wpilib()
  compile 'com.google.code.gson:gson:2.8.2'
  compile project(':common')
  compile group: 'com.flybotix', name: 'highfrequencyrobots', version: HFRVER  
}
